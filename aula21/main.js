const num1 = prompt('Digite o primeiro valor');
const num2 = prompt('Digite o segundo valor');

const num1Convertido = Number(num1);
const num2Convertido = Number(num2);
let res = num1Convertido + num2Convertido;
alert(`O resultado da soma dos dois valores é de ${res}`)